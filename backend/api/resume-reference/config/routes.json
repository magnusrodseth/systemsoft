{
  "routes": [
    {
      "method": "GET",
      "path": "/resume-references",
      "handler": "resume-reference.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-references/count",
      "handler": "resume-reference.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/resume-references/:id",
      "handler": "resume-reference.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/resume-references",
      "handler": "resume-reference.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/resume-references/:id",
      "handler": "resume-reference.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/resume-references/:id",
      "handler": "resume-reference.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
