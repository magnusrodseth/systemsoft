import { styled } from "../../stitches.config";

const Icon = styled("div", {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  color: "$mauve12",
  fontSize: "$5xl",
});

export default Icon;
